/** Tooltip when site-page discovery is disabled (one run per company). */
export const PLAYWRIGHT_WEBSITE_URL_DISCOVERY_RUN_DISABLED_TITLE =
  "Site page URL discovery can only be run once per company. Edit same-domain URLs manually if needed.";
