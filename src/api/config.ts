/**
 * Reserved for optional future localhost tooling only.
 * Graphics and CRM data persist via Express → Supabase / JSON vault.
 */
export const API_BASE_URL =
  process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://127.0.0.1:3000";
